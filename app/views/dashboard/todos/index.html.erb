<h1 class="text-xl font-semibold text-stone-500">
  <%= t('todos.list_all') %> <%= current_user.firm_setting.legal_name %>
</h1>

<div class="my-6"><%== pagy_nav @pagy %></div>

<%= render TableComponent.new(
      objects: @todos,
      list_attributes: %i(name dossier created_at due_at),
      class_name: "Todo",
      url_method: :url)
%>

<div class="my-6"><%== pagy_nav @pagy %></div>