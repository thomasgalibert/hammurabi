<nav class="flex flex-col sm:flex-row items-center justify-between py-10">
  <ul class="flex items-center space-x-4">
    <li>
      <%= link_to root_path do %>
        <%= image_tag "logo.svg", class: "w-auto h-16" %>
      <% end %>
    <li><%= link_to "Hammurabi", root_path,
            class: "text-3xl font-black" %></li>
  </ul>
  
  <ul class="flex space-x-4 mt-4 sm:mt-0 items-center">
    <% if user_signed_in? %>
      <li><%= link_to t('sessions.edit_password'), edit_password_path, class: "text-stone-600 border px-2 py-1.5 border-stone-300 rounded-md bg-white hover:text-stone-800 hover:shadow-md" %></li>
      <li><%= button_to t('sessions.logout'), session_path, method: :delete, class: "text-stone-600 border px-2 py-1 border-stone-300 rounded-md bg-white hover:text-stone-800 hover:shadow-md" %></li>
    <% else %>
      <li><%= link_to t('sessions.login'), new_session_path, class: "text-stone-600 border px-2 py-1 border-stone-300 rounded-md bg-white hover:text-stone-800 hover:shadow-md" %></li>
      <li><%= link_to t('sessions.signup'), new_registration_path, class: "text-stone-600 border px-2 py-1 border-stone-300 rounded-md bg-white hover:text-stone-800 hover:shadow-md" %></li>
    <% end %>
  </ul>
</nav>